# -----------------------------
# Vault Routing
# -----------------------------
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: vault-route
  namespace: vault
spec:
  parentRefs:
  - name: lab-gateway
    namespace: istio-system
  hostnames: ["vault.lab.internal"]
  rules:
  - backendRefs:
    - name: vault
      port: 8200
---
# -----------------------------
# ArgoCD Routing
# -----------------------------
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: argocd-route
  namespace: argocd
spec:
  parentRefs:
  - name: lab-gateway
    namespace: istio-system
  hostnames: ["argocd.lab.internal"]
  rules:
  - backendRefs:
    - name: argocd-server
      port: 80
---
# -----------------------------
# Rancher Routing
# -----------------------------
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: rancher-route
  namespace: cattle-system
spec:
  parentRefs:
  - name: lab-gateway
    namespace: istio-system
  hostnames: ["rancher.lab.internal"]
  rules:
  - backendRefs:
    - name: rancher
      port: 80
---
# -----------------------------
# Longhorn Routing
# -----------------------------
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: longhorn-route
  namespace: longhorn-system
spec:
  parentRefs:
  - name: lab-gateway
    namespace: istio-system
  hostnames: ["longhorn.lab.internal"]
  rules:
  - backendRefs:
    - name: ak-outpost-authentik-proxy
      namespace: authentik
      port: 9000
---
# -----------------------------
# Grafana Routing
# -----------------------------
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: grafana-route
  namespace: grafana
spec:
  parentRefs:
  - name: lab-gateway
    namespace: istio-system
  hostnames: ["grafana.lab.internal"]
  rules:
  - backendRefs:
    - name: grafana
      port: 80
---
# -----------------------------
# Kiali Routing
# -----------------------------
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: kiali-route
  namespace: istio-system
spec:
  parentRefs:
  - name: lab-gateway
    namespace: istio-system
  hostnames: ["kiali.lab.internal"]
  rules:
  - backendRefs:
    - name: ak-outpost-authentik-proxy
      namespace: authentik
      port: 9000
---
# -----------------------------
# Authentik Routing
# -----------------------------
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: authentik-route
  namespace: authentik
spec:
  parentRefs:
  - name: lab-gateway
    namespace: istio-system
  hostnames: ["authentik.lab.internal"]
  rules:
  - backendRefs:
    - name: authentik-helm-release-server
      port: 80
---
# -----------------------------
# Forgejo Routing
# -----------------------------
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: forgejo-route
  namespace: forgejo-system
spec:
  parentRefs:
  - name: lab-gateway
    namespace: istio-system
  hostnames: ["forgejo.lab.internal"]
  rules:
  - backendRefs:
    - name: forgejo-http
      port: 3000
---
# -----------------------------
# Forgejo SSH Routing
# -----------------------------
apiVersion: gateway.networking.k8s.io/v1alpha2
kind: TCPRoute
metadata:
  name: forgejo-ssh-route
  namespace: forgejo-system
spec:
  parentRefs:
  - name: lab-gateway
    namespace: istio-system
    sectionName: ssh
  rules:
  - backendRefs:
    - name: forgejo-ssh
      port: 22