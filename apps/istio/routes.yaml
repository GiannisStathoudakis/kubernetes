# -----------------------------
# Vault Routing
# -----------------------------
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: vault-route
  namespace: vault
spec:
  parentRefs:
  - name: lab-gateway
    namespace: istio-system
  hostnames: ["vault.lab.internal"]
  rules:
  - backendRefs:
    - name: vault
      port: 8200
---
# -----------------------------
# ArgoCD Routing
# -----------------------------
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: argocd-route
  namespace: argocd
spec:
  parentRefs:
  - name: lab-gateway
    namespace: istio-system
  hostnames: ["argocd.lab.internal"]
  rules:
  - backendRefs:
    - name: argocd-server
      port: 80
---
# -----------------------------
# Rancher Routing
# -----------------------------
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: rancher-route
  namespace: cattle-system
spec:
  parentRefs:
  - name: lab-gateway
    namespace: istio-system
  hostnames: ["rancher.lab.internal"]
  rules:
  - backendRefs:
    - name: rancher
      port: 80
---
# -----------------------------
# Longhorn Routing
# -----------------------------
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: longhorn-route
  namespace: longhorn-system
spec:
  parentRefs:
  - name: lab-gateway
    namespace: istio-system
  hostnames: ["longhorn.lab.internal"]
  rules:
  - backendRefs:
    - name: longhorn-frontend
      port: 80